# 簡単！真剣！お絵描きピクセルバトル！ — 属性・戦闘効果仕様書

| 項目 | 内容 |
|------|------|
| ドキュメント版 | 1.1 |
| 最終更新 | 2026-06-12 |
| 対象 | ウェブプロトタイプ → 将来 Unity / API |
| 関連 | [プロトタイプ開発指示書](./PROTOTYPE_DEVELOPMENT_SPEC.md) / [経済仕様書](./ECONOMY_SPEC.md) |
| 実装 | `src/config/attributes.ts`（表示マスタ）、`src/game/`（戦闘エンジン） |

---

## 目次

1. [目的とスコープ](#1-目的とスコープ)
2. [共通ルール](#2-共通ルール)
3. [属性一覧・解放](#3-属性一覧解放)
4. [各属性の詳細](#4-各属性の詳細)
5. [状態異常・バフ](#5-状態異常バフ)
6. [ターン処理順](#6-ターン処理順)
7. [同率時の解決順（tie-break）](#7-同率時の解決順tie-break)
8. [相互作用早見表](#8-相互作用早見表)
9. [実装段階](#9-実装段階)
10. [未決定・TBD](#10-未決定tbd)

---

## 1. 目的とスコープ

### 1.1 目的

- 全属性（剣・盾を含む **10 属性**）の **戦闘中の行動・効果・数値** を単一の参照源として定義する。
- [プロトタイプ開発指示書 §8](./PROTOTYPE_DEVELOPMENT_SPEC.md#8-戦闘ルール) の骨格（5枚・前衛2/後衛3・1ターン）に載る **属性レイヤ** を記述する。

### 1.2 プロトタイプ現行との関係

| 区分 | 内容 |
|------|------|
| **実装済み** | 剣・盾・力・弓・両・毒（`attack`〜`poison`） |
| **仕様確定・未実装** | 癒・氷・嵐・忍 |
| **属性決定（作成時）** | 現行: hash + 色比率（剣/盾のみ）。将来: AI 漢字1文字（[PROTOTYPE §5.4](./PROTOTYPE_DEVELOPMENT_SPEC.md#54-属性決定)） |
| **表示** | 漢字1文字 + `ATTRIBUTE_META`（`src/config/attributes.ts`） |

### 1.3 前衛・後衛の基本

- **前衛**: 原則 **近接攻撃** が可能（属性ごとに追加行動あり。§4 参照）。
- **後衛**: 属性ごとに行動可否が異なる（§4 参照）。
- **将来方針**: 前衛でも「近接 OR 特殊」の **1ターン1行動** に統一する案あり（現仕様は **属性ごと** に前衛/後衛の行動を定義）。

---

## 2. 共通ルール

### 2.1 1ターン1行動（行動カード）

| 項目 | 内容 |
|------|------|
| 原則 | 各陣営 **1ターンに1枚だけ** 行動カードを選ぶ |
| 行動の種類 | 近接攻撃・弓攻撃・両・嵐・癒・盾付与 など（属性による） |
| 排他 | 嵐・弓・両・近接は **そのターンの行動カード1枚の攻撃行動**。他カードは同ターン行動しない |
| 付与系 | 毒・氷は **近接の行動カード** で戦った相手に、攻撃と同時に付与（毒は一方的な反撃でも付与 — §4.6） |
| 忍（後衛） | ステルス中は **行動カードに選べない**（前衛に上がってから行動） |

### 2.2 ダメージの基準

| 種別 | 基準 |
|------|------|
| **原則** | バトル中の攻撃・付与ダメージ（近接、弓、両副攻撃、嵐など）は **付与者の currentBp** |
| **例外（毒 DoT）** | 各毒スタックは **付与時点の付与元 currentBp × 30%**（§5.1） |
| **回復上限** | 癒による回復は **maxBp まで**（オーバーヒールなし） |
| **表示** | 対人戦で相手が読みやすいよう、攻撃は原則 **今カードに表示されている BP** を基準とする |

### 2.3 近接攻撃と反撃

| 状況 | 攻撃側 | 反撃側 |
|------|--------|--------|
| **相打ち**（同じ2枚が互いに近接の行動カード） | currentBp **100%** | currentBp **100%** |
| **一方的**（片方だけが近接の行動カード、または別の相手を攻撃中の前衛が横から殴られる） | currentBp **100%** | currentBp **50%** |

- 相打ち判定: 同一ターンに互いが相手を近接ターゲットに選んだ **同じ2枚** の組（実装: `bidirectional` フラグ）。
- 弓・両副攻撃・嵐は **反撃ルールの対象外**（無反撃または別規定）。

### 2.4 盾（全属性共通）

| 項目 | 内容 |
|------|------|
| 効果 | **1回限り**、ダメージを 0 にして消費 |
| 嵐 | 盾があれば **盾消費・ダメージ 0** |
| 攻撃側の盾 | 近接で攻撃した側が盾を持つ場合、反撃ダメージ 0・攻撃側盾消費（剣/盾 Lv0 ルール維持） |

---

## 3. 属性一覧・解放

### 3.1 一覧

| 内部 ID | 漢字 | 解放 Lv | tie-break 優先度 | 実装 |
|---------|------|---------|------------------|------|
| `attack` | 剣 | 1 | 1 | 済 |
| `defense` | 盾 | 1 | 2 | 済 |
| `power` | 力 | 6 | 3 | 済 |
| `bow` | 弓 | 11 | 4 | 済 |
| `dual` | 両 | 16 | 5 | 済 |
| `poison` | 毒 | 21 | 6 | 済 |
| `heal` | 癒 | 26 | 7 | 未 |
| `ice` | 氷 | 31 | 8 | 未 |
| `storm` | 嵐 | 36 | 9 | 未 |
| `ninja` | 忍 | 41 | 10 | 未 |

- tie-break: 数値が **大きいほど先** に解決（§7）。
- Lv46 以降: 将来属性用（[PROTOTYPE §5.9](./PROTOTYPE_DEVELOPMENT_SPEC.md#59-ユーザーレベル解放5nm-統一)）。

### 3.2 作成時の属性

- **新規作成時のみ** 属性決定。編集では変更不可（[PROTOTYPE §5.8](./PROTOTYPE_DEVELOPMENT_SPEC.md#58-新規作成と編集)）。
- 候補属性は **作成時点で解放済み** のもののみ（将来 AI 属性時も同様）。

---

## 4. 各属性の詳細

### 4.1 剣（attack）— 実装済み

| 位置 | 行動 |
|------|------|
| 前衛 | **近接攻撃**（行動カード）。敵前衛の左右どちらか |
| 後衛 | 行動不可 |

- 特殊能力なし。§2.3 の近接・反撃ルールに従う。

### 4.2 盾（defense）— 実装済み

| 位置 | 行動 |
|------|------|
| 前衛 | **近接攻撃** または **盾付与** のどちらか1つ（行動カード） |
| 後衛 | **盾付与** のみ（付与可能な対象がいる場合） |

| 項目 | 内容 |
|------|------|
| 初期盾 | バトル開始時から **盾1枚**（前衛・後衛問わず） |
| 盾付与 | 盾なしの **味方1体**（**自分不可**）。**1戦闘1回**（防御カードごと） |
| 操作（前衛） | 行動カード選択後、敵前衛タップ＝攻撃、盾なし味方タップ＝盾付与 |

### 4.3 力（power）— 実装済み

| 位置 | 行動 |
|------|------|
| 前衛 | **近接攻撃**のみ |
| 後衛 | 行動不可 |

| 項目 | 内容 |
|------|------|
| 特殊能力 | **なし**（高 BP で戦う） |
| BP 算出 | 他属性と同様 hash＋密度でレンジ内を決定したうえで、**中心 BP に `POWER_BP_RATIO`（1.5）** を乗じたレンジを使用（[PROTOTYPE §5.5](./PROTOTYPE_DEVELOPMENT_SPEC.md#55-bp-決定)）。レア倍率は他属性と同タイミング |

### 4.4 弓（bow）— 実装済み

| 項目 | 内容 |
|------|------|
| BP 算出 | 剣比 **`BOW_BP_RATIO`（0.6）**。レア倍率は他属性と同タイミング |
| 行動 | **弓攻撃のみ**（前衛・後衛問わず） |
| 弓攻撃 | 生存している **敵1体** を選択。**反撃なし**。与ダメ = currentBp **100%** |
| 対象 | 敵前衛・後衛のいずれも可。**忍のステルス中は狙えない** |
| 近接 | **不可** |

### 4.5 両（dual）— 実装済み

| 位置 | 行動 |
|------|------|
| 前衛 | **両攻撃**（行動カード：主近接＋副攻撃） |
| 後衛 | 行動不可 |

| 項目 | 内容 |
|------|------|
| 主対象 | 通常の近接（§2.3）。毒・氷の付与も **主対象のみ** |
| 副対象 | **もう片方の敵前衛**（主対象と **同時**） |
| 副ダメージ | 自分の currentBp × **50%**、**無反撃** |
| 副対象の盾 | あれば **盾消費**（ダメージ 0） |
| 忍 | ステルス中は **近接の主対象にも副対象にもならない** |
| UI（実装） | 主攻撃・副攻撃それぞれに矢印（副攻撃は細線） |

### 4.6 毒（poison）— 実装済み

| 位置 | 行動 |
|------|------|
| 前衛 | **近接攻撃** ＋ 毒付与 |
| 後衛 | 行動不可 |

| 項目 | 内容 |
|------|------|
| BP 算出 | 剣比 **`POISON_BP_RATIO`（0.85）**。レア倍率は他属性と同タイミング |
| 付与（攻撃側） | 毒が **近接の行動カード** として攻撃した **主対象** に付与（両の副対象には付与しない） |
| 付与（被攻撃側） | **一方的近接**を受け、**反撃ダメージ > 0** のとき **攻撃側** に付与（相打ち `bidirectional` は対象外） |
| 付与時 BP | その近接1回の **ダメージ適用前** の付与元 `currentBp`（撃破直前でも付与前 BP で固定） |
| スタック | **付与元カード（cardId）ごと** に独立して重ねがけ可。`damagePerTurn` は付与時に固定 |
| DoT | **毎ターン開始時**（行動選択の **前**）、各スタックの `damagePerTurn` を合算（§6） |
| 持続 | 対象が倒れるまで。**付与者が撃破されても** 既存スタックは継続 |
| 近接本体 | currentBp 基準（§2.2） |
| UI（実装） | 毒スタック数の ☠ バッジ、DoT 時のダメージ表示・BP 演出。DoT で前衛が空いたら **行動選択前に補充必須** |

### 4.7 癒（heal）

| 位置 | 行動 |
|------|------|
| 後衛 | **回復**（行動カード） |
| 前衛 | **近接攻撃** または **回復** のどちらか1つ |

| 項目 | 内容 |
|------|------|
| 回復量 | 回復時点の **自分の currentBp** |
| 対象 | **既にダメージを受けた味方のみ**（満タン不可）。**自分も可** |
| 上限 | **maxBp まで**（オーバーヒールなし） |
| 回数 | **1枚あたり1戦闘2回まで** |
| タイミング | 行動カードで癒を選んだターン、**盾・攻撃より先** に解決（§6） |
| 凍結中の味方 | **回復可能**（回復を受けると忍のステルス解除対象になりうる — §5.3） |

### 4.8 氷（ice）

| 位置 | 行動 |
|------|------|
| 前衛 | **近接攻撃** ＋ 凍結付与 |
| 後衛 | 行動不可 |

| 項目 | 内容 |
|------|------|
| 付与 | 近接で **戦った** 相手に凍結（主対象のみ） |
| 効果 | **次ターン開始〜次ターン終了** まで行動不能。 **次々ターン** から行動可能 |
| 再凍結 | 凍結中を再度近接 → **タイマーリセット**（再び「次ターン丸ごと凍結」） |
| 攻撃対象 | 凍結中も **攻撃・回復の対象に選べる** |
| 毒 DoT | 凍結中も **毎ターン開始時に毒ダメージは入る** |

### 4.9 嵐（storm）

| 位置 | 行動 |
|------|------|
| 後衛 | **嵐**（行動カード、**1戦闘1回**） |
| 前衛 | **近接攻撃** または **嵐** のどちらか1つ |

| 項目 | 内容 |
|------|------|
| ダメージ | 自分の currentBp × **25%** を、生存敵から **ランダム2体** に（**必ず別ユニット**。1体しかいなければ1体） |
| 盾 | 盾があれば **盾消費・ダメージ 0** |
| 忍 | ステルス中も **当たりうる**（ダメージ＋ステルス解除） |

### 4.10 忍（ninja）

| 位置 | 行動 |
|------|------|
| 後衛 | **ステルス維持**（行動カードに **選べない**） |
| 前衛 | **近接攻撃**のみ |

| 項目 | 内容 |
|------|------|
| ステルス中 | 敵の **攻撃対象にならない**（弓・両副対象・嵐のランダム対象からも除外） |
| 初回無反撃 | **1戦闘1回限り**。自分が **初めて敵を攻撃した** 近接は無反撃 |
| ステルス解除 | ① **自ら敵を攻撃した瞬間**（その1回は無反撃） ② **外部から何らかの効果・ダメージを受けた瞬間**（近接・弓・嵐・毒 DoT・癒・盾付与・凍結など） |
| 再ステルス | **なし** |

---

## 5. 状態異常・バフ

### 5.1 毒（スタック）

- `BattleUnit.poisonStacks`: `{ sourceCardId, damagePerTurn }[]`（`src/types/battle.ts`）。
- **付与**: `grantPoisonStack`（`src/game/poisonCombat.ts`）。`damagePerTurn = round(付与時 currentBp × POISON_DOT_RATIO)`。`POISON_DOT_RATIO = 0.3`（`balance.ts`）。
- **DoT**: `startNextTurn` → `applyPoisonDoT`（`src/game/turnPhases/poisonDoT.ts`）。ターン見出しログの直後、行動選択前に適用。
- 毒 DoT で BP 0 になったユニットは即撃破。空き前衛があれば §6 の補充を行動選択前に行う。

### 5.2 凍結

- 行動カード選択不可のターンを `frozenUntilTurn` 等で管理。
- 攻撃対象としては選択可能（§4.8）。

### 5.3 ステルス（忍）

- `stealthActive` 等。解除条件は §4.10。
- **癒・盾・嵐・ダメージ全般** で解除。

### 5.4 盾

- 盾属性の初期盾・付与に加え、全属性が `hasShield` を持ちうる。

---

## 6. ターン処理順

### 6.1 1ターン全体の流れ

**ターン N（N ≥ 2）の開始時** — 行動選択の **前**:

```
① ターン見出し（--- TURN N ---）
② 毒 DoT（全生存ユニット。§5.1）
③ 撃破処理（DoT による撃破）
④ 前衛補充（DoT で空いた前衛。CPU 自動・プレイヤー操作。候補1枚は自動）
⑤ 行動カード選択（各陣営1枚）
   ※ 凍結中のユニットは選択不可（未実装）
```

**行動確定後**（従来のターン解決）:

```
⑥ 癒（行動カードで癒を選んだ側）
   ※ currentBp 高い順。同率は §7

⑦ 盾付与（行動カードで盾付与を選んだ側）
   ※ currentBp 高い順。同率は §7

⑧ 攻撃フェーズ
   弓・近接・両 等（行動カードの currentBp 高い順。同率は §7）
   ※ 近接は §2.3。複数近接は bidirectional 判定後、1戦闘ずつ
   ※ 嵐は未実装

⑨ 撃破処理（攻撃による撃破）

⑩ 前衛補充（戦闘による空き。CPU 自動・プレイヤー操作）
   ※ 弓が前衛に上がっても BP は変化しない
```

- **ターン 1**: ①は見出しログのみ（既存スタックなし）。②〜④はスキップ相当。
- 実装: ①〜④は `startNextTurn` + UI 再生（`useBattle` の `turnStartPoison`）。⑥〜⑩は `resolveTurn`。
- 前ターン終了後は **⑨⑩**（戦闘起因の撃破・補充）のあと、次ターンの **①〜④** に入る。

### 6.2 補充のタイミング（まとめ）

| きっかけ | タイミング |
|----------|------------|
| 毒 DoT | **行動選択前**（§6.1 ④） |
| 近接・弓・両などの攻撃 | **行動解決後**（§6.1 ⑩） |

---

## 7. 同率時の解決順（tie-break）

癒・盾付与・攻撃フェーズなど **currentBp 順** を使う箇所で、BP が同率のとき:

| 順位 | 条件 |
|------|------|
| 1 | **currentBp**（高い方が先） |
| 2 | **属性優先度**（§3.1 の表。大きい方が先） |
| 3 | **レア度**（高い方が先: SR > R > N …） |
| 4 | **限界突破 ★**（多い方が先） |
| 5 | **ランダム**（50%。対人戦の公平性のため） |

---

## 8. 相互作用早見表

|  | 忍（ステルス） | 盾 | 凍結 | 毒 DoT |
|--|--------------|-----|------|--------|
| **弓（後衛）** | 狙えない | 通常 | 対象可 | — |
| **両・副攻撃** | 副対象にならない | 副対象で盾消費 | — | 主対象のみ付与（副対象へ毒なし） |
| **毒（一方的反撃）** | — | — | — | 反撃ダメージ > 0 なら攻撃側へ付与 |
| **嵐** | 当たりうる・解除 | 盾消費 | 対象可 | — |
| **癒** | 受けたら解除 | — | 対象可 | — |
| **凍結中** | — | — | — | **DoT は入る** |

---

## 9. 実装段階

| 段階 | 属性・内容 | 状態 |
|------|------------|------|
| 0 | 剣・盾 | 済 |
| 1 | 力（`POWER_BP_RATIO`） | 済 |
| 2 | 弓（`BOW_BP_RATIO`、弓攻撃のみ） | 済 |
| 3 | 両（主近接＋副前衛50%・副攻撃矢印UI） | 済 |
| 4 | 毒（`POISON_BP_RATIO` / `POISON_DOT_RATIO`、DoT・バッジ・ターン開始処理） | 済 |
| 5 | 癒 | 未 |
| 6 | 氷 | 未 |
| 7 | 嵐 | 未 |
| 8 | 忍 | 未 |

- 数値定数: `src/config/balance.ts`
- 表示: `src/config/attributes.ts`（`ATTRIBUTE_META`）
- 戦闘: `src/game/`（`resolveTurn`, `startNextTurn`, `turnPhases/*`）

---

## 10. 未決定・TBD

| # | 項目 |
|---|------|
| 1 | 力属性の `ATTRIBUTE_POWER`（デッキ戦力 flatBonus） |
| 2 | 未実装属性（癒・氷・嵐・忍）の `ATTRIBUTE_META` 最終文案 |
| 3 | 前衛でも全属性「近接 OR 特殊」に **統一** するか（§1.3 将来方針） |
| 4 | AI 属性決定への移行時期 |

---

## 改訂履歴

| 版 | 日付 | 内容 |
|----|------|------|
| 1.0 | 2026-06-12 | 初版。10属性・ターン順・tie-break・5n+m 解放 Lv 対応を反映 |
| 1.1 | 2026-06-12 | 力・弓・両・毒の実装反映。毒 DoT をターン開始（行動選択前）に移動、一方的反撃付与・BP係数・§6 補充タイミングを追記 |

---

*属性の戦闘効果に関する記述は **本書を正** とする。数値調整後は §10 と `src/config/balance.ts` を同期すること。*
