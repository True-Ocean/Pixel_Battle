# 簡単！真剣！お絵描きピクセルバトル！ — プロトタイプ開発指示書

| 項目 | 内容 |
|------|------|
| ドキュメント版 | 2.6 |
| 最終更新 | 2026-06-20 |
| 関連 | [属性・戦闘効果仕様](./ATTRIBUTE_SPEC.md) / [経済仕様](./ECONOMY_SPEC.md) / [経済ロードマップ](./ECONOMY_ROADMAP.md) |
| 対象 | ウェブプロトタイプ（React + Vite + TypeScript） |
| 本番想定 | Unity（クライアント）＋ API / DB（将来） |

---

## 目次

1. [目的とスコープ](#1-目的とスコープ)
2. [技術スタックと環境](#2-技術スタックと環境)
3. [将来アーキテクチャ（参考）](#3-将来アーキテクチャ参考)
4. [機能一覧（プロトタイプ）](#4-機能一覧プロトタイプ)
5. [カード作成仕様](#5-カード作成仕様)
6. [デッキと永続化](#6-デッキと永続化)
7. [戦闘前フロー](#7-戦闘前フロー)
8. [戦闘ルール](#8-戦闘ルール)
9. [属性・戦闘効果](#9-属性戦闘効果)
10. [CPU 仕様](#10-cpu-仕様)
11. [敗北・仮ロスト](#11-敗北仮ロスト)
12. [実装構成の指針](#12-実装構成の指針)
13. [定数・調整パラメータ](#13-定数調整パラメータ)
14. [未実装・将来拡張](#14-未実装将来拡張)
15. [決定履歴サマリ](#15-決定履歴サマリ)

---

## 1. 目的とスコープ

### 1.1 目的

- 企画「簡単！真剣！お絵描きピクセルバトル！」の**コア体験**を、低コストなウェブプロトタイプで検証する。
- **5枚制限・前衛2/後衛3の配置・ターン戦闘・自作ドット絵・カード名**の感触を確認する。
- 戦闘ロジックを **UI と分離** し、将来の **Unity 移植** および **サーバー検証** に再利用しやすくする。

### 1.2 プロトタイプで作るもの

| 区分 | 内容 |
|------|------|
| プラットフォーム | ブラウザ（React + Vite + TypeScript） |
| データ保存 | `localStorage`（サーバーなし） |
| 対戦 | CPU のみ |
| 対戦 | CPU のみ |
| カード属性 | **戦闘実装済み**: 剣・盾・力・弓・両・毒・癒・氷・嵐・忍（全10属性）。定義は [ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md)。**ユーザーレベル**に応じて色・キャンバス・ツール・属性が解放（[§5.9](#59-ユーザーレベル解放5nm-統一)） |
| ユーザープロフィール | 初回セットアップでユーザー名登録。Lv.1 から開始。ヘッダーに Lv + プログレスバー。バトル報酬で EXP 蓄積・レベルアップ（上限 **Lv50**） |

### 1.3 プロトタイプで作らないもの

- ★（スター）・**AI 属性決定**（[§5.4.2](#542-将来方針ai-による漢字1文字属性) 将来方針・未実装）・創作ボーナスの拡張（パレット・キャンバス解放は実装済み）
- メール・パスワード認証（将来 API / DB で実装。型・仕様のみ予約可）
- 全10属性の戦闘実装は完了（仕様は [ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md)）
- AI による漢字1文字属性決定（[§5.4.2](#542-将来方針ai-による漢字1文字属性未実装)）
- 本番ロスト（カードデータの永久削除）
- 課金・殿堂入り・オンライン非同期対戦
- 画像ファイルのサーバー保存

---

## 2. 技術スタックと環境

### 2.1 採用技術

| 層 | 技術 |
|----|------|
| フロントエンド | React 18+ / Vite / TypeScript |
| 状態・UI | React（コンポーネント + state） |
| 永続化 | `localStorage` |
| パッケージ管理 | npm |

### 2.2 開発環境

- **Node.js**: インストール済み想定（確認時 v24.14.1）
- **エディタ**: Cursor / VS Code
- **初回セットアップ（実装時）**:
  ```bash
  npm create vite@latest . -- --template react-ts
  npm install
  npm run dev
  ```
- **スマホ実機確認**: 開発サーバーは `host: true` で LAN 上の端末からアクセス可能（例: `http://192.168.x.x:5173`）
- **PWA**: `vite-plugin-pwa` によりホーム画面追加に対応。アイコン生成は `npm run generate:pwa-icons`

### 2.3 推奨ディレクトリ構成

```
src/
  components/     # 画面（キャンバス、デッキ、戦闘UI、ConfirmDialog、SetupScreen）
  config/         # balance.ts / palette.ts（定数・パレット配置）
  game/           # 純粋な戦闘ロジック（UI非依存）
  canvas/         # 16×16 描画・flood fill・チェッカーボード
  card/           # 属性抽選（rollAttribute）・BP 算出・create / update / attributeChange
  storage/        # localStorage 読み書き
  user/           # ユーザープロフィール・EXP 加算（UI非依存）
  types/          # Card, Attribute, BattleState, UserProfile 等
```

**原則**: `game/` と `card/` は React に依存しない関数・型のみとし、Unity（C#）移植時は仕様書 + テストケースを参照して再実装する。

---

## 3. 将来アーキテクチャ（参考）

本プロトタイプの決定事項は、以下の将来構成と矛盾しないこと。

| 段階 | 技術 | 役割 |
|------|------|------|
| プロトタイプ | React + Vite | ルール検証・バランス・UX |
| 本番クライアント | Unity | バトルアニメ・パーティクル・ドット演出 |
| サーバー | API + DB（例: Postgres） | ユーザーデータ、非同期対戦、戦績 |
| ストレージ方針 | ピクセルは RLE 等の文字列 + メタデータのみ | 画像ファイルはサーバーに保存しない |

---

## 4. 機能一覧（プロトタイプ）

### 4.1 ユーザーフロー

0. **初回セットアップ**（未登録時のみ）：ユーザー名入力 → Lv.1 から開始
1. **マイデッキ**画面（最大5枚表示・並べ替え・削除・タップで編集）
2. **イメージ作成** / **カード編集**：解放済みキャンバス・パレット・ツールで描画。新規は **カード名入力** → 生成（属性は **ランダム抽選**）。編集は **イメージ・名前・キャンバス拡大** → 保存（[§5.8](#58-新規作成と編集)）
3. **バトル**タブ：CPU戦 → デッキ選択 → 5枚を **前衛2枚・後衛3枚** に配置 → バトル開始
4. ターン制戦闘（CPU 対戦）
5. 勝敗 → EXP 加算（勝敗に関係なく **現在レベル + 倒した敵枚数**）→ ロスト抽選（該当時）→ 戦利品選択（勝利時）
6. **戦績**タブの **バトル履歴** から過去対戦の再戦（[§11.4](#114-バトル履歴再戦)）

### 4.2 画面要件（最低限）

- **初回セットアップ**：ユーザー名入力（1〜16文字）+ **はじめる**。メール・パスワードは表示しない
- ヘッダーに **ユーザー名 · Lv.N · プログレスバー**（EXP 数値は非表示。セットアップ完了後）。**三本線メニュー**は水色プロフィールバー内右端
- **マイデッキ**一覧（5枚上限、新規作成・編集・削除・並べ替え）
- ドット描画エディタ（パレット・ツール）+ 新規時は名前入力 + 生成（属性・BP は自動算出。属性は **解放済みからランダム**）。編集はイメージ・名前・キャンバス拡大・BP は保存時再算出
- 戦闘前：5枚配置 UI（前衛2・後衛3）+ 30秒準備タイマー
- 5枚通しの盤面、ターンごとの行動カード選択、対象選択、**段階的な攻撃演出**（ダメージ表示 → BPカウントダウン → 次の攻撃）
- 中央ガイダンス帯（1行表示）、陣営別勝敗表示（WIN / LOSE）、終了後フッター操作
- プロトタイプ注記：「カードは保存されています（仮ロスト）」

### 4.3 マイデッキ画面 UI

| 項目 | 内容 |
|------|------|
| 画面タイトル | **マイデッキ** |
| サブ情報 | `最大5枚 · N / 5`。仮ロスト中は「仮ロスト演出中（データは保存済み）」を表示 |
| 通常時の操作 | カード行をタップ → **カード編集**画面へ。サブに「タップで編集」を表示 |
| 編集モード | ヘッダ右の **編集** / **完了** トグル。サブに「並べ替え・削除」を表示 |
| 並べ替え | ドラッグ＆ドロップ、または各行の **↑** / **↓** |
| 削除 | 行左の **−** → 確認ダイアログ（「戦績も失われます」）。確定で `localStorage` から削除 |
| 編集中の制限 | カードタップ編集・**新規カード作成**・**バトル** を無効化 |
| フッター操作 | **思い出アルバム**（左下・セピア色）/ **新規カード作成**（5枚未満）/ **並べ替え** / **バトル**（5枚揃ったときのみ有効） |
| 5枚未満 | 「戦闘にはあと N 枚必要です（合計 5 枚）」を表示 |

#### 4.3.1 カード詳細（属性変更・デッキ操作）

| 項目 | 内容 |
|------|------|
| 属性枠 | **アイコン → 説明文 → ▼**（1行・縦中央） |
| ▼ メニュー | **属性リタッチ**（200px）/ **属性セレクト**（💎20）。ロスト中は非表示 |
| 完了表示 | リタッチ: `BP {旧}→{新}` + 保有コイン。セレクト: `{ariaName}になりました！` + **BP** 大表示 + 保有ジュエル（[ECONOMY §16.17](./ECONOMY_SPEC.md#1617-通貨残高の結果表示)） |
| **active 時ボタン** | **編集**（全幅）→ **思い出アルバムに保存｜削除**（2列）→ **閉じる**（全幅） |
| **active 削除タップ** | 3択 — 思い出アルバムに保存 / 削除する / キャンセル |
| **ロスト時ボタン** | 復活上限内: **復活｜思い出アルバムに保存｜削除**。上限到達: **保存｜削除** ＋「復活上限に達しました」 |
| **`reviveCount`** | **復活 n/3** 表示（[ECONOMY §6.3.1](./ECONOMY_SPEC.md#631-復活全レア)） |
| 詳細 | [ECONOMY_SPEC §8.4](./ECONOMY_SPEC.md#84-属性変更属性リタッチ属性セレクト)、[ATTRIBUTE_SPEC §3.3](./ATTRIBUTE_SPEC.md#33-作成後の属性変更) |
| 実装 | `DeckCardDetailCard` / `DeckCardDetailOverlay` / `AttributeRetouchModal` / `AttributeSelectModal` / `MemoryAlbumDialogs` |

#### 4.3.2 思い出アルバム画面

| 項目 | 内容 |
|------|------|
| 導線 | マイデッキ左下 **思い出アルバム** |
| レイアウト | 1行5枚タイル。次行は **行を解放する 💎200** |
| 詳細 | タップで閲覧。**思い出アルバムから削除 💎1**（全幅）→ **閉じる** |
| 経済 | [ECONOMY_SPEC §14.1](./ECONOMY_SPEC.md#141-思い出アルバムプロトタイプ実装済) |
| 実装 | `MemoryAlbumScreen`, `MemoryAlbumDetailOverlay` |

### 4.4 イメージ作成 / カード編集画面 UI

| 項目 | 新規（イメージ作成） | 編集（カード編集） |
|------|----------------------|---------------------|
| 画面タイトル | **イメージ作成** | **カード編集** |
| 確定ボタン | **カード作成**（確認ダイアログ後に保存） | **保存**（即時） |
| 戻る | **マイデッキに戻る**（未保存内容は破棄） | 同左 |
| イメージ（`pixels`） | 編集可 | 編集可 |
| カード名 | 入力可 | **常時編集可**（保存時にリネーム課金。[ECONOMY_SPEC §8.2](./ECONOMY_SPEC.md#82-カード名変更編集画面)） |
| 属性（漢字1文字） | 作成時に **ランダム抽選**（[§5.4.1](#541-プロトタイプ現行解放済み属性からランダム)） | **表示のみ**（編集画面では変更不可）。変更は詳細の **リタッチ/セレクト**（[§4.3.1](#431-カード詳細属性変更)） |
| キャンバスサイズ | 解放済みから選択 | **拡大のみ変更可**（px 消費。作成時より小さいサイズ不可。保存前に元サイズへ戻せば拡大コスト 0。[§5.8](#58-新規作成と編集)） |
| 使用可能な色 | レベル解放色 ＋ ショップ購入済みの追加色（[§5.6](#56-パレットとレベル解放)） | 同左（レベルアップ・ショップ購入後に新色で仕上げ可能） |

**レイアウト（上から）**

1. 画面タイトル（この画面ではアプリ共通ナビ **非表示**）
2. **キャンバスサイズ選択** + **ミニプレビュー**（描画エリア上）
3. **描画ツール**（縦・左）+ **ドットキャンバス**（右）
4. **カラーパレット**（キャンバス下・**2行×10列**）
5. **カード名**（新規は入力欄、編集は **常時編集可**）+ **属性表示**（編集時・読み取り専用）+ 説明文
6. フッター固定：**カード作成** / **保存**（必要時 **拡大** 🪙 / **リネーム** 🪙・💎 内訳表示）、**マイデッキに戻る**
7. 編集保存後（BP 変化あり）: `EditorSaveBpConfirmModal` — BP 増減 + 課金時の保有コイン/ジュエル表示

**説明文（カード名の下）**

- **新規作成**:
  > あなたが描いたイメージとカード名から、カードのステータスが自動生成されるよ
- **編集**:
  > イメージ・カード名・キャンバスサイズ（拡大のみ）を変更できます。属性はカード詳細のリタッチ/セレクトで変更できます。保存すると、現在のレベルに応じて BP が再計算されます。

**カラーパレット（キャンバス下・2行×10列）**

- 計20スロット（index 0〜19）。**列ごとに用途が揃う**（白の下に灰、青の下に薄青 等）
- index 0〜7（白〜桃）: レベル解放。未解放は **ロック枠** ＋ 必要 Lv をツールチップ表示
- index 8〜19: ショップ追加色。未購入は **鍵アイコン**。Lv50 未満は「Lv50で購入可能」、Lv50+ はタップで **購入モーダル**（`PaletteUnlockModal`）
- 解放スケジュール・価格帯は [§5.6](#56-パレットとレベル解放) 参照

**描画ツール（キャンバス左・縦並び）**

- `IDEAL_TOOL_ORDER`（`src/config/editorTools.ts`）の理想順で、**解放済みかつ実装済み** のみ上から表示
- Lv1: ペン・消しゴム・塗りつぶし・クリア / Lv7: 元に戻す / Lv12: やり直す / Lv17: 直線 / Lv22: 選択 / Lv27: 矩形 / Lv32: 円
- **スポイト**はコード実装済みだが **UI 非表示**（パレット色が大幅増加した将来に解放予定）

| ツール | 挙動 |
|--------|------|
| ペン | 選択色でマスを塗る（Lv1 解放。ドラッグで連続） |
| 色スウォッチ | **ブラシ色** を選択（ツールは切り替えない） |
| 消しゴム | タップしたマスを `null`（透明）に |
| 塗りつぶし | 同色相連領域を **flood fill** |
| クリア | 全マスを空に（チェッカーボード背景 + 「クリア」ラベル） |
| 元に戻す | 直前の編集状態に戻す（Lv7 解放） |
| やり直す | 元に戻した操作を取り消す（Lv12 解放。新規編集で future はクリア） |
| 直線 | ドラッグで2点間に直線（Lv17 解放。選択色で描画） |
| 選択 | 矩形範囲をドラッグで指定 → **フロート表示** → ドラッグで複製先を指定して確定（**元の絵は残る**。Lv22 解放） |
| 矩形 | ドラッグで矩形の外枠（Lv27 解放） |
| 円 | ドラッグで楕円の外枠（Lv32 解放） |
| スポイト | （将来）タップしたマスの色を取得。**現 UI 非表示** |

**キャンバスサイズ選択**

- **ドロップダウン** で **16 〜 解放済み上限** の整数から選択（未解放サイズは非表示またはロック）
- 新規作成のデフォルトは **現在レベルで使える最大サイズ**
- 既存カード編集時は **作成時 `canvasSize` 以上** のみ選択可（[§5.8](#58-新規作成と編集)）

**キャンバス**

- 選択サイズの正方形グリッド（初期 16×16、最大 Lv50 で 36×36）。未塗りマスは **チェッカーボード**
- 画面上のキャンバス枠は CSS でほぼ固定。解像度が上がると 1 マスは小さくなる
- **カード表示**（デッキ・バトル等）は外枠サイズ固定。`canvasSize` に応じてセルがスケール（`image-rendering: pixelated`）
- サイズ変更時は既存ピクセルを **左上基準** で新グリッドにコピー
- ペン / 消しゴムはドラッグで連続操作（塗りつぶしは1タップ）
- 直線・矩形・円はドラッグでプレビュー → 離して確定（選択色）
- 選択は範囲ドラッグでフロート化 → ドラッグで複製先を指定して確定（元位置は保持。他ツールへ切替時はその位置で複製確定）

**バリデーション（確定前）**

| 条件 | メッセージ |
|------|------------|
| カード名が空 | カード名を入力してください |
| 塗りマス 0 | 1マス以上塗ってください |
| デッキ上限（新規のみ） | デッキは最大 5 枚です |

**新規作成の確認ダイアログ**

- タイトル: 「カードを作成しますか？」
- 本文: 「このイメージと名前でカードを作成します。よろしいですか？」
- 確定: **作成する**（primary スタイル） / キャンセル

**編集時のデータ保持**

- `updateCardFromDrawing`：`id`・`name`・`attribute`・`rarity`・`stars`・`wins`・`losses`・`reviveCount`・`createdAt`・`canvasSize` は維持
- `pixels` のみ更新可能。`bp` は **保存時のユーザーレベル** と絵内容（`bpBlend`）に応じて再算出
- 詳細は [§5.8 新規作成と編集](#58-新規作成と編集) を参照

### 4.5 戦闘準備・戦闘画面 UI

| 項目 | 内容 |
|------|------|
| 画面上部タイトル | **「バトル準備」「バトル！」は表示しない**（アプリ共通ヘッダも非表示） |
| 中央ガイダンス | 盤面中央の **1行帯**（ピンク〜紫のグラデーション）。ターン・操作ヒントを `TURN N：…` 形式で1行表示 |
| 準備時の文言例 | `バトル準備：カードを配置してください（残り N 秒）` |
| バトル時の文言例 | `TURN 1：カードを選択` / `TURN 3：攻撃先か盾先を選択` / `TURN 8：バトル終了！` |
| スロット枠 | 配置中は空スロットに点線枠＋「前衛」「後衛」ラベル。双方5枚配置完了後は **カードありスロットの枠を非表示**。バトル中も同様 |
| 敵手札・配置 | 上部に敵手札5枚（裏面）を表示。CPU は手札からスロットへ **1枚ずつ順番に配置アニメ** |
| 配置完了ボタン | 画面下部に固定（`ランダム配置` / `準備完了`）。盤面・手札は必要に応じてスクロール |
| 防御・前衛の操作 | **行動ボタンは置かない**。行動カード選択後、**敵前衛タップ＝攻撃**、**盾なし味方タップ＝盾付与** |
| 試合終了 | 中央は `TURN N：バトル終了！` のみ。**WIN / LOSE は各陣営エリア中央に大きく重ね表示**（上＝敵、下＝味方）。対人戦では各クライアントの上下を「自分／相手」に割り当てる |
| 終了後操作 | 3列グリッド: **もう一度対戦** / 中央ガイダンス（ターン文言＋履歴再戦時は **+N px** インライン）/ **バトルログ**。盤面はスクロール可能 |

### 4.6 戦績・バトル履歴

| 項目 | 内容 |
|------|------|
| 画面 | **戦績** タブ（`RecordsScreen`） |
| サブタブ | **バトル履歴** / ランキング（準備中） |
| 一覧 | 直近 **20件**（`BATTLE_HISTORY_MAX`）。相手デッキのサムネ5枚は **レアリティ枠色** 付き |
| 詳細 | 対戦日時・勝敗・戦力・相手デッキ5枚。「**もう一度対戦する**」で履歴再戦フローへ |
| 記録対象 | **通常 CPU 戦のみ**（履歴再戦は追記しない） |
| 実装 | `battleHistory.ts`, `BattleHistoryList`, `BattleHistoryDetailOverlay` |

### 4.7 バトルハブ・デッキ選択

| 項目 | 内容 |
|------|------|
| バトルハブ | `BattleHubScreen` — CPU戦 → デッキ選択へ |
| デッキ選択 | `BattleDeckSelectScreen` — 複数デッキから選択・スロット内入替・タップで詳細 |
| 常時ヒント | 「カードをタップして別のカードをタップすると入替」「同じカードをもう一度タップで詳細画面」 |
| 通常 CPU 戦 | バトル可能デッキ（ロストなし5枚）のみ開始可。黄色注意は出さない |
| 履歴再戦 | `deckReadinessMode="historyRematch"`。**5枚揃い**ならロスト含め可。戻る **バトル履歴に戻る** |

---

## 5. カード作成仕様

### 5.1 入力

| 項目 | 必須 | 説明 |
|------|------|------|
| `name` | ○（新規のみ入力） | プレイヤーが付けるカード名（最大32文字）。**編集時も入力欄で変更可**（保存時課金） |
| `pixels` | ○ | `canvasSize` の正方形グリッド。色は **現在レベルで解放済みのパレット色** のみ有効。未塗りは `null` |
| `canvasSize` | ○（新規のみ選択） | 解放済みサイズから選択。**編集時は作成時の値を維持** |

### 5.2 色比率の算出

塗られたマスのみを対象とする（未塗りは分母に含めない。実装時にコメントで固定すること）。

```
R = 赤マス数 / 塗ったマス合計
W = 白マス数 / 塗ったマス合計
B = 黒マス数 / 塗ったマス合計
（R + W + B = 1）
```

### 5.3 シード（hash）

- **入力文字列**: `name` + 正規化した `pixels`（JSON 文字列など、実装で1形式に固定）
- **処理**:  cryptographic hash または十分な衝突耐性の hash → 数値化（例: 0〜1 の `hashAttack`, `hashDefense`）
- **性質（プロトタイプ）**: 同じ名前 + 同じ絵 → **常に同じ** 属性・BP（決定論）
- **性質（将来・AI）**: 作成時に1回だけ判定し、結果を `Card.attribute` に保存。以降は編集しても属性は変わらない

### 5.4 属性決定

#### 5.4.1 プロトタイプ現行（解放済み属性からランダム）

**方針**: 新規作成時、**ユーザーレベルで解放済みの全属性** から权重付きランダム抽選。実装: `src/card/rollAttribute.ts` → `createCardFromDrawing`。

| 項目 | 内容 |
|------|------|
| 候補 | `getUnlockedAttributes(userLevel)` |
| 基本权重 | 各属性 **1.0** |
| 直近解放ブースト | スケジュール上 **最後に解放された属性** のみ **1.1**（`ATTRIBUTE_ROLL_RECENT_BOOST`） |
| 色・hash | **属性決定には不使用**（BP 算出の hash + 密度は [§5.5](#55-bp-決定) のまま） |
| 編集画面 | 属性は **変更不可**（表示のみ） |
| 作成後の変更 | カード詳細の **属性リタッチ**（200px）/ **属性セレクト**（💎20）— [ATTRIBUTE_SPEC §3.3](./ATTRIBUTE_SPEC.md#33-作成後の属性変更) |

- **新規作成時のみ** 自動決定。編集保存では属性を変更しない（[§5.8](#58-新規作成と編集)）。

~~**旧プロトタイプ（廃止）**: 3色バランス + hash による剣/盾二択（`deriveAttribute`）~~

#### 5.4.2 将来方針（AI による漢字1文字属性）

カード作成ボタン確定時（**新規作成のみ**）に、以下を入力として AI（またはサーバー経由の同等ロジック）へ渡し、**漢字1文字**の属性を返させる。表示は `ATTRIBUTE_META`（`src/config/attributes.ts`）の `label` に対応させる。

**入力（コスト最小化を想定）**

| 項目 | 内容 |
|------|------|
| カード名 | プレイヤー入力 |
| イメージの見た目 | ASCII ピクセル表現などコンパクトな形式（将来 Vision API 併用も可） |
| 色数 | 使用した色の種類数 |
| カラーバランス | 各色の比率・色調の雰囲気 |
| 塗りピクセル数・密度 | 塗ったマス数、`canvasSize` に対する密度 |
| キャンバスサイズ | `canvasSize` |
| 候補属性 | **作成時のユーザーレベルで解放済み**の属性のみ（漢字リスト） |

**方針**

- 色合いのバランス・イメージの意味・カード名を総合して属性を決める（色だけでは決めない）
- 結果はカードに保存し、**編集では再判定しない**
- API 失敗時はプロトタイプと同様の hash + 色比率フォールバックを検討
- **色のレベル解放と属性の追加**: 色を Lv N で解放し、属性を Lv N+1 で追加する案を検討中（詳細は実装時に確定）

### 5.5 BP 決定

- **方針**: `hash` + **密度**（塗ったマス / グリッド総マス数）で、属性ごとの **BP レンジ** 内の値を決定。
- 密度が高いほどレンジの **上限側** に寄せる（線形補間。`src/config/balance.ts` の `computeCardBaseBp`）。
- **レンジの中心**は **ユーザーレベル** と属性でスケール（`getUserBaseBp`）。

| タイミング | BP の算出 |
|------------|-----------|
| **新規作成** | 作成時の `userLevel` でレンジを決定。`bpBlend`（hash + 密度）でレンジ内の値を決める |
| **編集保存** | **保存時の `userLevel`** でレンジを再スケール。`attribute`・`rarity` は固定。`bpBlend` は絵の変更で変わりうる |

- レベルアップ後に既存カードを編集保存すると、同じ絵でも BP レンジが伸びる。絵を直すと `bpBlend` も変化しうる。
- プロトタイプ用の具体レンジ（例、要プレイテスト）:

| 属性 | BP レンジ（案） |
|------|-----------------|
| attack | 70 〜 100 |
| defense | 55 〜 85 |

※上記数値は指示書時点の仮値。`src/config/balance.ts` で一元管理すること。

### 5.6 パレットとレベル解放

`PALETTE_16`（実質20色・2×10 グリッド）の全色定義。index 0〜7 はレベル解放、index 8〜19 は Lv50 以降のショップ永久解放。

**1行目（index 0〜9）**

| index | 色 | 解放 |
|-------|-----|------|
| 0 | 白 | 1（初期） |
| 1 | 黒 | 1 |
| 2 | 赤 | 1 |
| 3 | 青 | **5** |
| 4 | 黄 | **15** |
| 5 | 緑 | **25** |
| 6 | 橙 | **35** |
| 7 | 桃 | **45** |
| 8 | 紫 | ショップ（Lv50+・2000px） |
| 9 | 濃い緑 | ショップ（Lv50+・2000px） |

**2行目（index 10〜19）— 上段の同列と用途が揃う**

| index | 色 | 上段の対応 | 用途の目安 | 解放 |
|-------|-----|-----------|-----------|------|
| 10 | 灰 | 白 | 影・雲・金属のハイライト | tier2 |
| 11 | 茶 | 黒 | 土・木・髪の暗部 | tier1 |
| 12 | 薄赤 | 赤 | 肌・唇の淡い色 | tier2 |
| 13 | 薄青 | 青 | 空・水・氷の淡色 | tier2 |
| 14 | 薄黄 | 黄 | 光・星・淡い暖色 | tier2 |
| 15 | 薄緑 | 緑 | 草地の明部・若葉 | tier2 |
| 16 | 薄橙 | 橙 | 肌の影・夕焼けの淡色 | tier2 |
| 17 | 薄桃 | 桃 | 頬・花の淡いピンク | tier2 |
| 18 | 薄紫 | 紫 | 夕暮れ・魔法の淡色 | tier2 |
| 19 | 赤茶 | 濃い緑 | 土・レンガ・暗い暖色 | tier1 |

**色コード（`PALETTE_16`、index 順）**

`#ffffff` `#000000` `#ff0000` `#2222ff` `#ffdd00` `#44cc44` `#ff8800` `#ff44ff` `#8844ff` `#1a7a3a` `#aaaaaa` `#886644` `#ffaaaa` `#aaccff` `#ffffcc` `#ccffcc` `#ffddaa` `#ffccff` `#ddccff` `#aa4422`

- レベル解放ルール: **L ≡ 5 (mod 10)** かつ L ≥ 5（**5, 15, 25, 35, 45**）で index 3 から **1 色ずつ** 追加
- Lv50 時点のレベル解放色数: **8色**（白・黒・赤・青・黄・緑・橙・桃）
- ショップ追加色: **12色**（紫・濃い緑・茶・赤茶 ＋ 薄色系8色）。`SaveData.paletteShopUnlocks` に購入済み index を保持
- ショップ価格帯（tier1/tier2）は **index ではなく色コード** で判定（`paletteShop.ts` の `PALETTE_SHOP_TIER1_HEX` / `TIER2_HEX`）。並べ替えしても価格がずれない
- 実装: `src/config/paletteUnlock.ts` / `src/config/paletteShop.ts` / `src/user/paletteShop.ts` / `src/components/ColorPalette.tsx` / `src/components/PaletteUnlockModal.tsx` / `src/components/ShopScreen.tsx`
- セーブ: `SAVE_SCHEMA_VERSION = 6`。`memoryAlbum`（思い出アルバム）を追加。旧並びからの `paletteShopUnlocks` は色コードで移行（schema v5 以前）

**色の役割**

| 段階 | 白・黒・赤 | 追加色（青・黄等） |
|------|------------|-------------------|
| プロトタイプ現行 | BP・レア抽選（使用色数）等 | 見た目・創作ボーナス（使用色数） |
| 将来（AI 属性） | 入力の一部 | 入力の一部。解放済み属性のみ候補（[§5.4.2](#542-将来方針ai-による漢字1文字属性)） |

**編集時の色**

- 編集画面のパレットは **現在のユーザーレベル** と **ショップ購入済み index** の和集合が利用可能
- レベルアップ・ショップ購入で増えた色を使い、同じカードのイメージを仕上げるのが編集の主な動機の一つ（[§5.8](#58-新規作成と編集)）
- 既存カードの `pixels` に廃止色（水色・黄緑）が残っている場合、描画時は **未解放色** として扱われる（ピクセルデータの自動変換は未実装）

**UI 配置**

- キャンバス下: **2行×10列**（計20スロット）。**列ごとに用途が揃う**（白の下に灰、青の下に薄青 等）
- ショップ価格帯（tier1/tier2）は **index ではなく色コード** で判定（並べ替え自由）
- キャンバス左: 描画ツール縦列（`getVisibleEditorTools`）
- ショップ色タップ: 購入モーダル（エディタ）または **ショップ画面**（メニュー → ショップ）から購入
- 開発メニュー（設定画面）: ショップ追加分の **全解放 / 未解放** 切替

### 5.7 キャンバスサイズとレベル解放

**方針**: 解放は **最大一辺長（px）の引き上げ**。新規作成時は **16 〜 現在の上限** の **任意整数** を **ドロップダウン** で選択（奇数も可）。編集時は作成時の `canvasSize` 固定（[§5.8](#58-新規作成と編集)）。

| 項目 | 内容 |
|------|------|
| 初期（Lv1） | 最大 **16** px |
| 解放間隔 | **L ≡ 3 (mod 5)** かつ L ≥ 8（**8, 13, 18, 23, 28, 33, 38, 43, 48**）のたび **+2 px** |
| Lv50 時点 | 最大 **34** px |
| Lv51+ | 将来拡張（例: L53 で 36 等） |

**解放レベルと上限の対応（Lv50 まで）**

| 解放 Lv | 最大一辺 |
|---------|----------|
| 1 | 16 |
| 8 | 18 |
| 13 | 20 |
| 18 | 22 |
| 23 | 24 |
| 28 | 26 |
| 33 | 28 |
| 38 | 30 |
| 43 | 32 |
| 48 | 34 |

- 実装: `src/config/canvasUnlock.ts`（**上限モデル・ドロップダウン UI に更新予定**）
- カードデータに `canvasSize`（整数）を保存。未設定の既存セーブは **16** として移行
- BP 密度: `塗り数 / (canvasSize²)`（奇数サイズも同式）

### 5.9 ユーザーレベル解放（5n+m 統一）

Lv1 は初期パック。Lv5 以降は **レベルの下一桁（L mod 5）** で報酬種別を決める（**5レベル帯に各1回**、キャンバスは帯内 L≡3 のみ）。

| 条件（L ≥ 5） | mod 5 | 報酬 | 例（L） |
|---------------|-------|------|---------|
| **L ≡ 5 (mod 10)** | 0 | **色** +1 / **Lv5: Lost 解禁** ＋ **護符 ×1** | 5, 15, 25, 35, 45 |
| **L ≡ 1 (mod 5)** | 1 | **属性** +1 | 6, 11, 16, 21, 26, 31, 36, 41 |
| **L ≡ 2 (mod 5)** | 2 | **描画ツール** +1 | 7, 12, 17, 22, 27, 32, 37 |
| **L ≡ 3 (mod 5)** | 3 | **キャンバス上限** +2 px | 8, 13, 18, 23, 28, 33, 38, 43, 48 |
| **L ≡ 4 (mod 5)** | 4 | **💎 ボーナス**（[ECONOMY_SPEC §10.4](./ECONOMY_SPEC.md#104-レベルアップ報酬)） | 9, 14, 19, 24, 29, 34, 39, 44 |
| **L ≡ 0 (mod 10)** | 0 | **Lv10: デッキ2解放** / **Lv20+: 汎用かけら ×10** | 20, 30, 40, 50 |

**全レベルアップ**: **無償ピクセル** ＋ **💎 少量**（[ECONOMY_SPEC §10.4](./ECONOMY_SPEC.md#104-レベルアップ報酬)）。

**デッキスロット解放**（[ECONOMY_SPEC §10.5](./ECONOMY_SPEC.md#105-デッキスロット解放)）:

| スロット | 条件 |
|----------|------|
| デッキ1 | 初期 |
| デッキ2 | **ユーザーレベル ≥ 10** |
| デッキ3〜5 | **💎 消費**（Lv10 以上） |

**属性解放順**（[ATTRIBUTE_SPEC §3](./ATTRIBUTE_SPEC.md#3-属性一覧解放)）:

| Lv | 属性 |
|----|------|
| 1 | 剣・盾 |
| 6 | 力 |
| 11 | 弓 |
| 16 | 両 |
| 21 | 毒 |
| 26 | 癒 |
| 31 | 氷 |
| 36 | 嵐 |
| 41 | 忍 |

**描画ツール解放**（`src/config/editorTools.ts`・L≡2）: Lv1 ペン/消しゴム/塗りつぶし/クリア → Lv7 元に戻す → Lv12 やり直し → Lv17 直線 → Lv22 選択 → Lv27 矩形 → Lv32 円 → Lv37 スポイト（UI 非表示予定）

**5レベル帯の例（Lv5〜9）**

| Lv | 報酬 |
|----|------|
| 5 | 色（青）＋ **Lost 解禁** ＋ **護符 ×1**（[ECONOMY_SPEC §4.2.1](./ECONOMY_SPEC.md#421-初心者保護ユーザーレベル--5)、§7.3） |
| 6 | 属性（力） |
| 7 | ツール（元に戻す） |
| 8 | キャンバス上限 +2 |
| 9 | 💎 ボーナス |

**5レベル帯の例（Lv10〜14）**

| Lv | 報酬 |
|----|------|
| 10 | **デッキ2 解放** |
| 11 | 属性（弓） |
| 12 | ツール（やり直し） |
| 13 | キャンバス上限 +2 |
| 14 | 💎 ボーナス |

**5レベル帯の例（Lv15〜19）**

| Lv | 報酬 |
|----|------|
| 15 | 色 |
| 16 | 属性 |
| 17 | ツール |
| 18 | キャンバス |
| 19 | 💎 ボーナス |

**5レベル帯の例（Lv20〜24）**

| Lv | 報酬 |
|----|------|
| 20 | **汎用かけら ×10** |
| 21 | 属性 |
| 22 | ツール |
| 23 | キャンバス |
| 24 | 💎 ボーナス |

### 5.8 新規作成と編集

新規作成と編集は、プレイヤーにとって別の目的を持つ。

| | **新規作成** | **編集** |
|--|--------------|----------|
| 目的 | 名前・属性・サイズを含め **ゼロから** 新しいカードを得る | 同じカードの **イメージを仕上げる**・名前変更・キャンバス拡大 |
| イメージ | 自由に描く | 修正可 |
| カード名 | 入力して決定 | **常時編集可** — 保存時にリネーム課金（初回100px・2回目以降💎1。[ECONOMY §8.2](./ECONOMY_SPEC.md#82-カード名変更編集画面)） |
| 属性 | 作成時 **ランダム抽選**（§5.4.1） | 編集画面では **変更不可**。詳細の **リタッチ/セレクト** で変更可（§4.3.1） |
| キャンバスサイズ | 解放済みから選択 | **拡大のみ**（`calcCanvasUpgradeCost` = 新²−旧² px、基準は編集開始時サイズ）。保存前に元サイズへ戻せば **0**。中間サイズ変更時は一度作成時サイズへ戻してから拡大モーダル |
| 色 | 現在レベルの解放色 | 現在レベルの解放色（レベルアップ後に新色で彩りを足せる） |
| BP | 作成時 `userLevel` | **保存時 `userLevel`** で再計算 |
| レア | 作成時抽選 | **維持**（編集では再抽選しない） |
| 戦績 | 0 で開始 | **維持** |

**プレイヤー動機（設計意図）**

- **編集**: レベルアップで増えた色で印象を変え、同じカードを育てる。名前も保存時に変更可。BP はレベルアップ後の保存で伸びる。
- **新規作成**: 別の名前・**ランダム属性**・より大きいキャンバスなど、ゼロからの新カード向き。
- **属性変更**: 詳細の **属性リタッチ**（px）で運試し、**属性セレクト**（💎）で確定狙い。

**実装**

- 新規: `createCardFromDrawing`（`rollAttribute`）
- 編集: `updateCardFromDrawing`（`src/card/createCard.ts`）
- 保存課金: `EditorScreen` + `App.tsx`（`updateCard` + `calcEditorSaveCharges`）
- 属性変更: `attributeChange.ts` / `DeckCardDetailOverlay`
- キャンバス拡大: `EditorScreen` + `calcCanvasUpgradeCost`（`src/config/economy.ts`）

---

## 6. デッキと永続化

### 6.1 デッキルール

- **1デッキあたり** 保有上限: **5枚**（各スロットは固定5枠）
- ストックなし（常に5枚デッキのみ）
- **思い出アルバム**: デッキ外の **閲覧専用** アーカイブ（初期5枠、行解放 **💎200**）。詳細は [ECONOMY_SPEC §14.1](./ECONOMY_SPEC.md#141-思い出アルバムプロトタイプ実装済)
- **デッキスロット**（複数デッキ）: 最大5スロット。解放条件は [ECONOMY_SPEC §10.5](./ECONOMY_SPEC.md#105-デッキスロット解放)（デッキ2=Lv10、3〜5=💎）
- プロトタイプ実装: `unlockedDeckCount`, `decks[]`, `BattleHubScreen`, `battleHistory[]`（最大20件）

### 6.2 保存形式（Q18-C）

**永続化する（`localStorage`）**

```typescript
interface Card {
  id: string;              // uuid 等
  name: string;
  pixels: (string | null)[][];  // canvasSize×canvasSize、例: "#FF0000" | null
  canvasSize: number;           // 16〜上限の整数（未設定セーブは 16）
  attribute: "attack" | "defense";
  bp: number;              // 戦闘時の最大BPとして使用
  wins: number;
  losses: number;
  reviveCount: number;     // プロトタイプでは 0 固定で可
  createdAt: string;       // ISO 8601
}

interface SaveData {
  user: UserProfile | null;  // null = 初回セットアップ未完了
  deck: Card[];              // 最大5件
}

interface UserProfile {
  username: string;          // 1〜16文字（trim 後）
  level: number;             // 累計 exp から算出（表示用に保存）
  exp: number;               // 累計 EXP（UI では数値非表示）
}
```

**EXP・レベルアップ（`src/user/level.ts`）**

- 1 戦の獲得 EXP = **現在レベル + 倒した敵カード枚数**（0〜5。勝敗不問）+ **上振れボーナス**（勝利・相手デッキ戦力が高いときのみ、最大 +3）
- 上振れ: 相手戦力 / 自分戦力 が **≥1.1 → +1**, **≥1.2 → +2**, **≥1.3 → +3**（参考。ゲーム結果への直接影響は EXP のみ）
- レベル L → L+1 に必要な EXP = **`EXP_LEVEL_BASE + L`**（初期 `BASE=10` → Lv.1→2 は 11）
- 累計 EXP 到達点: `totalExp(L) = (L-1)*BASE + (L-1)*L/2`
- 上限 **`MAX_USER_LEVEL = 50`**。降格なし
- UI: プログレスバーのみ（`expInLevel / expToNextLevel`）。レベルアップ時バー長は不変でレンジ切替

**デッキ戦力（`src/card/power.ts`・参考表示のみ）**

- カード戦力 = `round(BP × bpWeight[属性] + flatBonus[属性])`
- デッキ戦力 = 5枚のカード戦力合計（編成補正なし）
- Lv0: 攻撃 `{ bpWeight: 1, flatBonus: 0 }` / 防御 `{ bpWeight: 1, flatBonus: 20 }`（盾能力の反映）
- マイデッキ画面サブ情報に **戦力 N** を表示。バトル結果・ダメージ計算には使わない

**将来（本番・サーバー側・プロトタイプでは未保存）**

```typescript
interface UserAuth {
  email: string;
  // password はクライアントに保持しない
}
```

**永続化しない（戦闘セッション内メモリのみ）**

- 現在 BP
- 盾の付与状態・使用済みフラグ
- 麻痺・毒・凍結・ステルス等 — [ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md) 参照。戦闘セッション内メモリのみ

### 6.3 ピクセル保存の段階

| 段階 | 形式 |
|------|------|
| プロトタイプ | JSON 2次元配列 |
| 本番 / DB | RLE 等へ変換（同じ論理データ） |

### 6.4 デッキ操作（マイデッキ）

| 操作 | 仕様 |
|------|------|
| 追加 | **新規カード作成** → イメージ作成画面。上限 5 枚 |
| 編集 | カード行タップ → カード編集画面。**イメージ**・**名前**・**キャンバス拡大** が変更可。属性は詳細のリタッチ/セレクト。保存で `pixels`・`bp`・名前を更新（[§5.8](#58-新規作成と編集)） |
| 削除 | 詳細の **削除** → active 時は3択（保存/削除/キャンセル）。**💎1** ＋ px・かけら返還（二段階確認）。編集モードの **−** は従来どおり |
| 思い出アルバム | 詳細 **思い出アルバムに保存** または active 削除3択から。**無償**でデッキから除去 |
| 並べ替え | 編集モードでドラッグまたは ↑↓。順序は `localStorage` に保存 |
| 表示順 | 並べ替え後の配列順がマイデッキ一覧・戦闘前手札の初期順に反映 |
| 戦力 | サブ情報 `戦力 N`（5枚合計。参考値） |

---

## 7. 戦闘前フロー

1. プレイヤーの **5枚** と CPU の **5枚** を表示する。
2. スマホ縦持ちを最優先し、上部に敵カード列（裏面5枚）、下部に自分カード列（表面5枚）を小さく横並び表示する。
3. 中央盤面に、敵・自分それぞれ **前衛2スロット / 後衛3スロット** を表示する。
4. CPU は準備開始時に5枚の配置をランダム決定する。上部の敵手札（裏面5枚）から、空の敵スロットへ **前衛左 → 前衛右 → 後衛左 → 後衛中央 → 後衛右** の順で1枚ずつ配置アニメーションする。
5. プレイヤーは下部カード列からタップ方式で自分の5枚を中央の自分スロットへ配置する。
6. 準備時間は **30秒**。準備画面表示と同時にカウント開始する。
7. 5スロット未配置の間、ボタンは **「ランダム配置」** と表示する。押すと未配置カードのみを空きスロットへランダム配置し、すぐには開始しない。
8. 5スロットすべて配置済みになると、ボタンは **「準備完了」** に変わる。押すとバトル開始。
9. 30秒経過時は、未配置カードを空きスロットへランダム配置し、そのままバトル開始する。
10. バトル開始時、CPUカードを **前衛左 → 前衛右 → 後衛左 → 後衛中央 → 後衛右** の順に1枚ずつ表面へ反転する。ガイダンスは **「カードオープン」** とする。

### 7.1 配置と操作

| 項目 | ルール |
|------|--------|
| 初期配置 | 前衛2枚、後衛3枚 |
| 自分の盤面向き | 前衛2枚が中央寄り、後衛3枚が手前 |
| 敵の盤面向き | 前衛2枚が中央寄り、後衛3枚が奥 |
| 操作方式 | ドラッグではなくタップ方式 |
| 選択解除 | 選択中の同じカードを再タップ |
| 未配置カード → 空きスロット | 配置 |
| 未配置カード → 埋まっているスロット | 入れ替え（元カードは未配置列へ戻る） |
| 配置済みカード → 空きスロット | 移動 |
| 配置済みカード → 埋まっているスロット | 入れ替え |
| 戻し操作 | 配置済みカードを未配置列へ戻す専用操作はなし |
| 時間切れランダム配置 | 既に配置済みのカード位置は動かさない |
| 手札表示 | 未配置カードは属性ラベル付きで表示。配置のたびに手札は中央揃え |
| カウントダウン | 中央ガイダンス帯に1行で表示（`バトル準備：…（残り N 秒）`） |
| スロット枠 | 配置中は点線枠を表示。双方5枚配置完了後、カードありスロットの枠は非表示 |
| 画面タイトル | 「バトル準備」「バトル！」の画面上部タイトルは表示しない |

### 7.2 用語

| 用語 | 意味 |
|------|------|
| 行動カード | そのターンに自分が選んで行動させる1枚 |
| 前衛 | 近接攻撃できる2スロット |
| 後衛 | 通常の近接攻撃はできない3スロット |
| 墓地 | 撃破されたカードを小さく並べる表示領域 |
| 盾 | 1回だけ受けるダメージを0にして消費される状態 |

---

## 8. 戦闘ルール

### 8.1 試合の流れ

- **1ターン** を繰り返す。
- **終了条件**: どちらか一方の **5枚すべてが BP 0**。
- **勝利**: 相手の5枚すべて撃破。
- **敗北**: 自分の5枚すべて撃破。
- 撃破されたカードは盤面スロットから外れ、墓地へ移動する。元スロットは空きスロットになる。

### 8.2 ターン開始：行動カード選択

| ルール | 内容 |
|--------|------|
| 選択枚数 | 1ターンに行動できるカードは各陣営 **1枚だけ**（**行動カード**） |
| プレイヤー / CPU | 行動可能なカードから1枚を選ぶ |
| 同時解決 | 双方の選択を揃えたうえで [ATTRIBUTE_SPEC §6](./ATTRIBUTE_SPEC.md#6-ターン処理順) の順に解決 |
| 剣・前衛 | 近接攻撃（敵前衛） |
| 盾・前衛 | 近接 **または** 盾付与（§9 / ATTRIBUTE_SPEC） |
| 凍結 | 凍結中のカードは **行動カードに選べない** |

属性ごとの行動可否・対象は **[ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md)** を正とする。

### 8.3 攻撃対象（概要）

| 攻撃種別 | 対象 |
|----------|------|
| 近接攻撃 | 敵前衛（剣・盾・力・両・毒・氷・前衛の弓 等） |
| 弓 | 敵前衛・後衛（忍ステルス除く）。与ダメ currentBp **100%**・無反撃（ATTRIBUTE_SPEC §4.4） |
| 嵐 | 生存敵からランダム2体（別ユニット）。与ダメ currentBp **50%**・無反撃（ATTRIBUTE_SPEC §4.9） |

### 8.4 ターン処理：フェーズ

詳細・数値は **[ATTRIBUTE_SPEC §6](./ATTRIBUTE_SPEC.md#6-ターン処理順)** を参照。

```
【ターン開始 — 行動選択の前】
① ターン見出し・毒 DoT（§5.1）
② 撃破・前衛補充（DoT 起因。プレイヤーは補充必須）

【行動確定後】
③ 行動カード選択（各陣営1枚）※ UI 上は ①② の後
④ 癒（選ばれた側・先行・未実装）
⑤ 盾付与（選ばれた側）
⑥ 攻撃（弓・近接・両・嵐 等）
⑦ 撃破・墓地移動
⑧ 前衛補充（戦闘起因。弓前衛上がり時 BP 半減なし）
```

※ 詳細・番号対応は [ATTRIBUTE_SPEC §6](./ATTRIBUTE_SPEC.md#61-1ターン全体の流れ) を正とする。

### 8.5 前衛補充

配置を以下のように表す。

```
前衛: A B
後衛: C D E
```

| 空き前衛 | 上がれる後衛 |
|----------|--------------|
| A（左前衛） | C（左後衛）または D（中央後衛） |
| B（右前衛） | D（中央後衛）または E（右後衛） |

- D は左右どちらにも上がれる共有候補。
- 片側だけ空いている場合、対応候補がいなければ空きのまま次ターンへ進む。
- 両前衛が空いた場合も同じ対応ルールを守る。
- 複数候補がある場合、プレイヤーは補充順と移動先を自由に選べる。
- 候補が **1枚だけ** の場合は自動で前衛へ補充する（ユーザー操作不要）。
- 候補が **2枚** の場合のみ、ユーザーに選択を促す。
- CPU は対応候補からランダムに上げる。
- 補充操作はタップ方式: 上げる後衛カードをタップ → 同じカードを再タップで確定（1回目で選択、2回目で確定）。
- ガイダンス文言:
  - 未選択時: **「前衛に移動する後衛カードを選択」**
  - 選択後: **「もう一度タップで確定」**

### 8.6 撃破後

- BP 0 のカードはその試合では **もう選択できない**。
- 撃破カードは墓地に小さく表示し、グレーアウトまたは撃破ビジュアルを付ける。
- 自分と敵の墓地は分けて表示する。
- 盤面レイアウトは、準備中・バトル中・墓地移動後も固定する。

### 8.7 試合終了表示

| 項目 | 内容 |
|------|------|
| 中央ガイダンス | `TURN N：バトル終了！`（勝敗文言は出さない） |
| 敵陣営エリア | 墓地＋敵スロット全体の中央に **WIN** または **LOSE** を大きく重ね表示 |
| 味方陣営エリア | 味方スロット＋墓地全体の中央に **WIN** または **LOSE** を大きく重ね表示 |
| スタイル | グローは使わず、鮮やかな塗り＋太い縁取り（WIN＝黄、LOSE＝赤系） |
| 対人戦（将来） | 各クライアントで自分の陣営側に WIN/LOSE を表示。中央は双方共通の終了メッセージ |
| 終了後操作 | `デッキ` / `新規バトル` を画面下端に固定。盤面本体はスクロール可能 |

### 8.8 攻撃解決の詳細

#### 近接・反撃（剣/盾 Lv0 ＋ 全属性共通）

- **原則**: 攻撃ダメージは **currentBp** 基準（[ATTRIBUTE_SPEC §2.2](./ATTRIBUTE_SPEC.md#22-ダメージの基準)）。
- **相打ち**（同一2枚が互いに近接の行動カード）: 双方 **100%** の相互ダメージ。
- **一方的**: 攻撃側 **100%**、反撃 **50%**。
- 詳細・弓/両/嵐/毒/氷/忍は **[ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md)**。

#### 盾の相互作用

| 状況 | 結果 |
|------|------|
| 防御側のみ盾あり | 防御側はダメージ0、盾消費。攻撃側は通常通り反撃ダメージ |
| 攻撃側のみ盾あり | 攻撃側は反撃ダメージ0、盾消費。防御側は通常通りダメージ |
| 双方とも盾あり | 双方の盾が消え、双方BPは変わらない |

#### 複数攻撃の処理順

同一ターンに複数の近接攻撃が発生する場合:

1. 互いに同じ2枚のカードが攻撃し合う場合は **1戦闘** としてまとめる（両矢印）。
2. 残りの戦闘は **1件ずつ順番** に処理する。
3. 戦闘の優先順位は **攻撃を仕掛けたカードのBPが高い順**。
4. 同率なら双方の最大BPが高い順 → さらに同率なら防御側BPが高い順。

UI 演出もこの処理順に従い、各戦闘ごとに「ダメージ表示 → BPカウントダウン」を行う。

#### 複数攻撃の処理順

1. 互いに同じ2枚が近接の行動カード → **1戦闘**（相打ち / `bidirectional`）。
2. 残りは **1件ずつ** 処理。
3. 優先: **行動カードの currentBp 高い順**（同率は [ATTRIBUTE_SPEC §7 tie-break](./ATTRIBUTE_SPEC.md#7-同率時の解決順tie-break)）。

UI 演出もこの順に従う。

---

## 9. 属性・戦闘効果

**詳細は [ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md) を単一の参照源とする。**

### 9.1 プロトタイプ実装済み

| 属性 | 内部 ID | 概要 |
|------|---------|------|
| 剣 | `attack` | 前衛近接のみ |
| 盾 | `defense` | 初期盾 + 付与1回。前衛は近接 or 盾付与 |
| 力 | `power` | 前衛近接。BP ×1.5 |
| 弓 | `bow` | 前衛・後衛から弓攻撃のみ。BP ×0.6 |
| 両 | `dual` | 主近接 + 副前衛50%（無反撃） |
| 毒 | `poison` | 前衛近接 + 毒付与。DoT はターン開始時。BP ×0.85 |

- 表示: `ATTRIBUTE_META`（`src/config/attributes.ts`）
- 属性決定: **解放済みからランダム**（`rollAttribute`）+ レベル解放（[§5.4.1](#541-プロトタイプ現行解放済み属性からランダム)）
- 戦闘エンジン: `src/game/`（`resolveTurn`, `startNextTurn`）

### 9.2 仕様確定・未実装

癒・氷・嵐・忍 — 解放 Lv・行動・数値は **ATTRIBUTE_SPEC §3〜4**。

### 9.3 属性優先度（tie-break）

攻撃・癒・盾の同率時に使用。剣(1) … 忍(10)。**ATTRIBUTE_SPEC §3.1 / §7**。

---

## 10. CPU 仕様

### 10.1 配置

- CPU の5枚配置は、プロトタイプではランダム。
- 準備中は上部の敵手札に裏面5枚を表示し、空スロットへ1枚ずつ飛ばして配置する。
- 配置順は **前衛左 → 前衛右 → 後衛左 → 後衛中央 → 後衛右**。
- バトル開始時に同順で1枚ずつ表面へ反転する。

### 10.2 行動選択

- 初期実装では、CPUの通常ターン行動はランダムでよい。
- 実行可能な行動候補を列挙し、その中からランダムに1つ選ぶ。
- 攻撃行動の場合、敵前衛の生存カードからランダムに対象を選ぶ。
- 盾付与行動の場合、盾を持っていない生存味方からランダムに対象を選ぶ（**付与者自身は除く**）。
- 将来は、前衛保護・低BP保護・撃破可能攻撃などを評価するスコア式AIへ差し替えてよい。

### 10.3 前衛補充

- 空いた前衛に対して、対応候補からランダムに上げる。
- 候補がいなければ補充しない。

### 10.4 CPU デッキ

- プロトタイプ: 生成ルールまたは `src/data/cpu-decks.json` 等の **固定デッキ** で可。
- 将来: 他ユーザーの5枚メタデータを取得し、同じルールで CPU が操作。

---

## 11. 敗北・ロスト

> 経済実装後の本番ルールは [ECONOMY_SPEC.md §4.2](./ECONOMY_SPEC.md#42-敗北ペナルティv1--将来対人) に従う。初心者保護（Lv5 未満ロスト無効・Lv5 到達時の護符配布）は [§4.2.1](./ECONOMY_SPEC.md#421-初心者保護ユーザーレベル--5)。

### 11.1 本番（経済仕様）

| 項目 | 挙動 |
|------|------|
| トリガー | プレイヤー敗北（**ユーザーレベル ≥ 5**） |
| 対象 | 自軍 **墓地** から1枚（CPU: ランダム / 対人: 相手選択） |
| 結果 | 選ばれた1枚が `status: lost`（護符装備時は免れ。§7） |
| 復帰 | **復活**（px・上限3回）/ **思い出アルバムに保存** / **削除**（[ECONOMY §6.3](./ECONOMY_SPEC.md#63-復帰オプション)）。**降格復活なし** |
| Lv5 未満 | **ロストなし** — 敗北しても全カード `active` のまま |

### 11.2 プロトタイプ（仮ロスト・レガシー）

| 項目 | 挙動 |
|------|------|
| トリガー | 敗北時 |
| 演出 | ランダム1枚を「消滅」したように表示（グレーアウト等） |
| データ | **削除しない**（`localStorage` のデッキは維持） |
| 復帰 | **次の戦闘開始前**に、5枚すべて選択可能な状態へ **自動復帰** |
| UI | 「プロトタイプ：カードは保存されています」等の注記を表示 |

- 現行コードは本 Lost（§11.1）を採用。**Lv5 未満のロスト無効** は [ECONOMY_SPEC §4.2.1](./ECONOMY_SPEC.md#421-初心者保護ユーザーレベル--5) を正とする（未実装時は全レベルでロストが発生し得る）。

### 11.3 戦績

- 勝利 / 敗北時に `wins` / `losses` を更新してよい（仮ロストでも `losses` は +1 で可、仕様で固定）。
- **バトル履歴再戦**（[§11.4](#114-バトル履歴再戦)）ではカード・ユーザー戦績を **更新しない**。

### 11.4 バトル履歴再戦

経済ルールの正は [ECONOMY_SPEC §4.1.2](./ECONOMY_SPEC.md#412-バトル履歴再戦v1-プロトタイプ)。プロトタイプ実装のフロー:

1. 戦績 → **バトル履歴** → 対戦詳細 →「もう一度対戦する」
2. **再戦時のルール** モーダル（任意で「今日はこれ以降表示しない」）
3. **3回に1回**、ルール後・デッキ選択前にモックリワード広告
4. **デッキ選択**（`BattleDeckSelectScreen`, `historyRematch` モード）
5. バトル開始（相手デッキ構成維持・BP 再計算、マッチング演出スキップ）
6. 勝利時: **生存 px のみ** を中央ガイダンスにインライン表示。敗北時: ロスト抽選なし
7. 終了後「もう一度対戦」→ 手順2〜（広告判定含む）

- 実装: `historyRematch.ts`, `HistoryRematchRulesModal`, `MockRewardAdModal`, `App.tsx`（`historyRematchFlow`）

---

## 12. 実装構成の指針

### 12.1 戦闘エンジン API（案）

```typescript
// 例: 純関数。UI は state を渡し、結果 state を受け取る
function resolveTurn(state: BattleState, choices: TurnChoices): ResolveTurnResult;
function promoteUnit(state: BattleState, side: BattleSide, from: BoardPosition, to: BoardPosition): BattleState;
function isBattleOver(state: BattleState): "player" | "cpu" | null;
```

- `TurnChoices`: 行動カード、行動種別、攻撃対象、盾対象等
- ユニットテストを `game/*.test.ts` に書き、Unity 移植前の仕様固定に使う

### 12.2 戦闘状態・イベントログ（案）

```typescript
type BoardPosition =
  | "frontLeft"
  | "frontRight"
  | "backLeft"
  | "backCenter"
  | "backRight";

type BattleActionType = "meleeAttack" | "grantShield";

interface BattleFormation {
  slots: Record<BoardPosition, BattleUnit | null>;
  defeated: BattleUnit[];
}

interface BattleEvent {
  type:
    | "action_selected"
    | "shield_granted"
    | "attack"
    | "blocked"
    | "defeated"
    | "promoted";
}
```

- バトルログは通常画面には表示しない。
- リプレイ機能やお気に入り保存に備え、内部的にはログを維持する。
- 可能なら文字列ログだけでなく、演出・リプレイに使える構造化イベントログを保持する。
- 倒れたカードは `defeated` へ移動し、元スロットは `null` にする。

### 12.3 カード生成 API（案）

```typescript
function createCardFromDrawing(name: string, pixels: PixelGrid): Card;
function updateCardFromDrawing(existing: Card, name: string, pixels: PixelGrid): Card;
```

- `createCardFromDrawing`：新規 `id`・戦績 0 で生成。`attribute`・`rarity`・`bp` を算出
- `updateCardFromDrawing`：既存の `id` / `name` / `attribute` / `rarity` / `stars` / `wins` / `losses` / `reviveCount` / `createdAt` / `canvasSize` を保持し、`pixels` と `bp` のみ更新（`bp` は保存時 `userLevel` で再算出）

### 12.4 確認ダイアログ（共通）

`ConfirmDialog` を削除・作成などで共用する。

| 用途 | confirmVariant | 確定ラベル例 |
|------|----------------|--------------|
| カード削除 | `danger`（既定） | 削除する |
| カード作成 | `primary` | 作成する |

### 12.5 定数の一元管理

すべて `src/config/balance.ts`（パレット定義は `src/config/palette.ts`、ショップ価格は `src/config/paletteShop.ts`）に集約する。

### 12.6 画面・演出指針

#### レイアウト

- スマホ縦持ちを最優先レイアウトとする。
- PC では body 背景の上に **phone シェル** として中央配置する（`--phone-shell-w: 26.5rem`、バトル画面は `--phone-shell-battle-w: 22rem`）。
- 盤面レイアウトは準備中・バトル中・墓地移動後も固定する。
- 手札・配置スロット・バトル中・墓地の **すべてのカード** は、同一の内部レイアウト（パーツ位置・サイズ・マージン）で表示する。
- カードサイズは `--formation-card-w` / `--formation-card-h` で統一する。
- 中央はガイダンス表示と矢印・ダメージ演出のために使う。

#### イメージ作成 / カード編集画面

- 画面上部は共通ブランドタイトルのみ。**画面切替ナビは非表示**。
- パレット → キャンバス → カード名 → 説明文の間に **約 0.7〜0.85rem** の縦余白を確保する。
- フッター（**カード作成** / **保存**、**マイデッキに戻る**）は画面下部に固定配置する。

#### 戦闘準備・戦闘画面

- 戦闘準備・戦闘中は画面上部の「バトル準備」「バトル！」タイトルおよびアプリ共通ヘッダを **非表示** とする。
- 中央ガイダンスは **1行帯**（ピンク〜紫グラデーション、左右アクセントライン）。`TURN N：操作ヒント` 形式で常に1行。
- 準備フェーズは盤面・手札を `formation-setup-body` でスクロール可能にし、下部の `ランダム配置` / `準備完了` は固定。
- バトル終了後は盤面を `formation-battle-body` でスクロール可能にし、`デッキ` / `新規バトル` を下端固定。
- スロット枠は配置中のみ点線表示。双方配置完了後およびバトル中は、カードありスロットの枠を非表示（`is-frameless`）。
- 試合終了時、中央は `バトル終了！`、各陣営エリア（`formation-field-cpu` / `formation-field-player`）に WIN/LOSE を重ねる。

#### ガイダンス・操作

- ガイダンス文言は短い日本語で統一する。
- ターン表示は `TURN 1` のような英字表記とする。
- 行動カード選択中は選択カードをハイライト表示する。
- 選択中の同じカードを再タップすると選択解除する（専用の「選択解除」ボタンは置かない）。
- 防御属性・前衛で攻撃と盾付与の両方が可能なとき、**行動ボタンは出さない**。敵前衛タップで攻撃、盾なし味方タップで盾付与。
- 攻撃対象選択時は **敵前衛** を赤系でハイライトする。盾付与対象は **味方** を緑系でハイライトする。

#### 盾・バフ表示

- 盾状態はカード全体を覆う半透明表示ではなく、**カード左上の小さなバフアイコン**（🛡）で示す。
- 将来のバフ・デバフも同じ左上エリアに並べられる構造とする。
- BP は右上にライトな色のバッジ表示とする。

#### ターン演出（攻撃・盾）

1. **盾付与フェーズ**（該当時）  
   緑系の矢印で付与元→付与先を表示する。

2. **攻撃フェーズ**（1戦闘ずつ順番に）  
   各戦闘について次の順で再生する。

   | サブフェーズ | 内容 |
   |--------------|------|
   | ダメージ表示 | 関係するカードの中央に `−85` などのダメージ数値を大きく表示。盾で0の場合は非表示 |
   | BP変化 | ダメージ表示を消し、右上BPを **from→to** でカウントダウンアニメーション |
   | 次へ | 上記完了後、次の戦闘へ。全戦闘完了後にターン終了処理へ |

3. **矢印**
   - 攻撃は **カードの端から端** へ向ける（中央同士ではなく、矩形外周の交点を始点・終点とする）。
   - 相互攻撃の場合は両矢印（↔）表示。
   - 対象が異なる同時攻撃は片矢印2本を同時表示。
   - **BPカウントダウン中は攻撃矢印を非表示** にする（ダメージ表示フェーズでのみ表示）。
   - ダメージ表示は矢印レイヤーより上の専用レイヤーに描画し、矢印に隠れないようにする。

4. **その他**
   - 処理中のカードは揺れエフェクトを付ける。
   - バトルログは画面に出さない。内部ログ・構造化イベントはリプレイ用に維持する。

#### タイミング定数（案）

| 定数名 | 初期値 (ms) | 説明 |
|--------|-------------|------|
| `CLASH_MS.damage` | 420 | ダメージ数値表示時間 |
| `CLASH_MS.bp` | 520 | BPカウントダウン時間 |
| `CLASH_MS.exit` | 450 | ターン演出終了後の待機 |

#### resolveTurn 戻り値（攻撃演出用）

各 `AttackPlayback` には以下を含める。

- 攻撃側・防御側の位置（`fromSide` / `fromPosition` / `toSide` / `toPosition`）
- 防御側ダメージ・BP変化（`damage`, `bpFrom`, `bpTo`）
- 攻撃側ダメージ・BP変化（`attackerDamage`, `attackerBpFrom`, `attackerBpTo`）
- 相互攻撃フラグ（`bidirectional`）
- 処理後状態（`stateAfter`）

---

## 13. 定数・調整パラメータ

| 定数名（案） | 初期値 | 説明 |
|--------------|--------|------|
| `COLOR_WEIGHT` | 0.7 | 属性決定の色スコア比重 |
| `HASH_WEIGHT` | 0.3 | 属性決定の hash 比重 |
| `BLACK_ATTACK_WEIGHT` | 0.7 | 黒を攻撃スコアに載せる重み |
| `CANVAS_SIZE_DEFAULT` | 16 | 初期キャンバス・既存セーブ移行値 |
| `CANVAS_SIZE_MILESTONES` | — | **廃止予定** → 上限モデル（§5.7） |
| `CANVAS_MAX_AT_LEVEL` | 16+2×回数 | L≡3 解放ごと +2。Lv50 上限 **34** |
| `PALETTE_UNLOCK_LEVELS` | 5,15,25,35,45 | L≡5 (mod 10)（§5.6） |
| `ATTRIBUTE_UNLOCK_LEVELS` | 6,11,…,41 | L≡1 (mod 5)（§5.9 / ATTRIBUTE_SPEC） |
| `POWER_BP_RATIO` | 1.5 | 力属性 BP 中心（ATTRIBUTE_SPEC §4.3） |
| `DECK_MAX` | 5 | |
| `PALETTE_GRID_COLS` | 10 | パレット列数 |
| `PALETTE_GRID_ROWS` | 2 | パレット行数 |
| `PALETTE_EDITOR_COLOR_COUNT` | 20 | エディタ表示スロット数 |
| `PALETTE_SHOP_MIN_USER_LEVEL` | 50 | 追加色ショップ購入の最低レベル |
| `PIXEL_COST_PALETTE_SHOP_TIER1` | 2000 | 紫・濃い緑・茶・赤茶（各1回） |
| `JEWEL_COST_PALETTE_SHOP_TIER2` | 20 | 薄色系8色（💎支払い） |
| `PIXEL_COST_PALETTE_SHOP_TIER2` | 2200 | 薄色系8色（px支払い） |
| `PALETTE_UNLOCKED_COUNT_LV0` | 3 | Lv1 で選択可能な初期色数（白・黒・赤） |
| `FRONT_SIZE` | 2 | 前衛スロット数 |
| `BACK_SIZE` | 3 | 後衛スロット数 |
| `SETUP_TIME_LIMIT_SEC` | 30 | バトル準備時間 |
| `CPU_RANDOM_ACTION` | true | CPU初期実装はランダム行動 |
| `PROTOTYPE_FAKE_LOSS` | true | 仮ロストON |
| `USER_INITIAL_LEVEL` | 1 | 初回セットアップ時のユーザーレベル |
| `USER_INITIAL_EXP` | 0 | 初回セットアップ時の累計 EXP |
| `USERNAME_MAX_LENGTH` | 16 | ユーザー名上限 |
| `MAX_USER_LEVEL` | 50 | ユーザーレベル上限 |
| `EXP_LEVEL_BASE` | 10 | L→L+1 必要 EXP = BASE + L |
| `ATTRIBUTE_POWER` | 攻0/防+20 | カード戦力の属性係数（flatBonus） |
| `EXP_UPSET_RATIO_TIER1/2/3` | 1.1/1.2/1.3 | 上振れ勝利 EXP の戦力比しきい値 |
| `EXP_UPSET_BONUS_MAX` | 3 | 上振れ EXP 上限 |
| `CLASH_MS.damage` | 420 | ダメージ数値表示時間 (ms) |
| `CLASH_MS.bp` | 520 | BPカウントダウン時間 (ms) |
| `CLASH_MS.exit` | 450 | ターン演出終了待機 (ms) |

---

## 14. 未実装・将来拡張

以下は **本プロトタイプの対象外**、または **仕様のみ確定** の項目。

### 14.1 戦闘・属性

- **新属性8種**の戦闘実装 — 仕様は [ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md)
- **5n+m 解放**のコード反映（`paletteUnlock.ts` / `canvasUnlock.ts` / 属性解放）
- キャンバス **ドロップダウン UI**・上限モデル

### 14.2 カード・作成

- **AI 属性決定**（[§5.4.2](#542-将来方針ai-による漢字1文字属性)）
- ~~属性セレクト~~ — **💎 版は実装済み**（[ECONOMY §8.4](./ECONOMY_SPEC.md#84-属性変更属性リタッチ属性セレクト)）。将来: 属性選択チケット

### 14.3 その他

- メール・パスワード認証
- 非同期対戦・リプレイ
- 課金拡張
- Lv51+・キャンバス36+・追加属性

---

## 15. 決定履歴サマリ

| # | 質問テーマ | 決定 |
|---|------------|------|
| 1 | 最初の形 | ウェブ・プロトタイプ。バトルUIはスマホ縦持ち最優先 |
| 2 | 機能範囲 | B + 仮ロスト |
| 3 | 戦闘レイアウト | 5枚を前衛2・後衛3に配置 |
| 4 | 仮ロスト復帰 | 次戦闘前に自動復帰 |
| 5 | 1枚撃破時 | 墓地へ移動し、前衛空きは対応後衛から補充 |
| 6 | ターンの選び方 | 毎ターン、行動可能なカードを1枚選択 |
| 7 | フェーズ | 毒DoT → 選択 → 癒 → 盾 → 攻撃 → 撃破 → 補充（ATTRIBUTE_SPEC §6） |
| 8 | ループ | どちらか5枚全滅まで |
| 9 | 防御の対象 | 盾なしの生存味方1体。**自分は不可**。防御カードごとに1回限り |
| 10 | 配置操作 | タップ方式。30秒制限、未配置はランダム補完 |
| 11 | 技術 | React + Vite + TS |
| 12 | ピクセル保存 | まず JSON、後 RLE |
| 13–16 | 属性・BP | 3色バランス + hash、70:30、黒攻撃寄り、BPはhash+密度 |
| 17 | CPU | 初期配置・行動・補充はまずランダム |
| 18 | 永続化 | Card + deck、戦闘状態は非保存 |
| 19 | 演出・ログ | 画面ログ非表示。内部ログ維持。段階的攻撃演出（ダメージ→BP）・端から端の矢印・盾バフアイコン |
| 20 | 相互ダメージ | 相打ち100% / 一方50%反撃。currentBp基準（ATTRIBUTE_SPEC §2.3） |
| 21 | カードUI | 全シーンで同一内部レイアウト。BPバッジ・左上盾アイコン |
| 22 | 前衛補充UI | 候補1枚は自動、2枚はタップ2回で確定。段階的ガイダンス文言 |
| 23 | マイデッキ画面 | タイトル「マイデッキ」。編集モードで並べ替え・削除。タップでカード編集 |
| 24 | 描画エディタ | 2×10 パレット（レベル8色＋ショップ12色）、消しゴム/塗りつぶし/クリア。ショップ色は鍵表示・購入モーダル。新規は確認付き作成、編集は戦績維持 |
| 25 | アプリシェル | PC 中央 phone 幅。エディタ画面はナビ非表示・縦余白調整 |
| 26 | バトル画面ヘッダ | 「バトル準備」「バトル！」タイトル廃止。中央ガイダンス帯に1行統合 |
| 27 | 防御の盾付与 | 自分自身への付与不可。前衛防御は敵/味方タップで攻撃・盾付与（行動ボタンなし） |
| 28 | スロット枠 | 双方配置完了後・バトル中はカードありスロットの枠を非表示 |
| 29 | 試合終了UI | 中央は `バトル終了！`、WIN/LOSE は陣営エリア中央に分離表示（対人戦を見据える） |
| 30 | CPU配置演出 | 敵手札からスロットへ順次配置アニメ。開始時は順次オープン |
| 31 | 初回セットアップ | ユーザー名のみ。Lv.1 開始。EXP=レベル+撃破数。バー表示。認証は将来 |
| 32 | EXP・レベル | BASE+L 式。累計 exp 保存。UI は Lv+バーのみ |
| 33 | デッキ戦力 | 属性係数合計。参考表示。上振れ勝利 EXP のみ連動 |
| 34 | 新規 vs 編集 | 編集はイメージ＋名前（保存時課金）＋キャンバス拡大。属性は詳細でリタッチ/セレクト。BP は保存時ユーザーレベルで再算出 |
| 35 | 属性決定 | 作成時: **解放済みからランダム**（直近解放 +10%）。作成後: リタッチ200px / セレクト💎20 |
| 36 | 属性仕様 | 10属性を [ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md) に分離 |
| 37 | レベル解放 | 5n+m 統一（§5.9）。キャンバス上限34@Lv50 |
| 38 | 属性解放順 | Lv6力→11弓→16両→21毒→26癒→31氷→36嵐→41忍 |
| 39 | バトル履歴再戦 | 戦績非記録・ロストなし・生存 px のみ・3回に1回広告・日次 cap 対象外（ECONOMY §4.1.2） |
| 40 | 汎用かけら Lv20+ | **×10**/回（`UNIVERSAL_LIMIT_BREAK_LEVEL_REWARD`） |
| 41 | レベルアップ UI | px は数値→アイコン、L≡4 💎 は通常分とボーナスを分離表示 |
| 42 | 追加色ショップ | Lv50+。tier1=紫・濃い緑・茶・赤茶（各2000px）。tier2=薄色系8色（各💎20または2200px）。`paletteShopUnlocks` 永続化 |
| 43 | 属性ガチャ | 作成時 `rollAttribute`。詳細 ▼ でリタッチ/セレクト（完了時 BP・通貨残高表示）。編集リネームは保存一括課金 |
| 44 | 思い出アルバム | デッキ外閲覧専用。保存無償・行解放💎200・復活上限3・降格復活廃止。schema v6（ECONOMY §14.1） |

---

## 改訂履歴

| 版 | 日付 | 内容 |
|----|------|------|
| 2.7 | 2026-06-20 | §4.3 思い出アルバム・詳細ボタン配置、§6.1/§6.4 アルバム操作、§11.1 復帰オプション、schema v6 |
| 2.6 | 2026-06-20 | §4.3.1・§4.4 完了モーダル・保存コスト内訳（拡大/リネーム）、§5.8 編集時キャンバス戻し挙動 |
| 2.5 | 2026-06-20 | §5.4 属性ランダム抽選・§5.8 編集/属性変更・§4.3.1 詳細 UI・§4.4 エディタ刷新（名前常時編集・保存コスト） |
| 2.4 | 2026-06-17 | §4.4・§5.6 パレット20色・ショップ追加色実装（2×10配置、濃い緑/赤茶、廃止色移行、schema v5） |
| 2.3 | 2026-06-17 | §4.4・§5.8 リネーム実装・編集時キャンバス拡大、編集前広告（暫定） |
| 2.2 | 2026-06-16 | §4.6〜4.7 バトル履歴・デッキ選択、§11.4 履歴再戦、汎用かけら×10、レベルアップ UI、ヘッダーメニュー配置、戦闘終了3列 UI |
| 2.1 | 2026-06-14 | §5.9 レベル報酬（Lv10=デッキ2、💎、Lv20=限界突破）、§6.1 複数デッキスロット、§5.8 リネーム方針。ECONOMY_ROADMAP 連携 |
| 2.0 | 2026-06-12 | [ATTRIBUTE_SPEC.md](./ATTRIBUTE_SPEC.md) 新設。5n+m 解放（§5.9）、キャンバス上限モデル（§5.7）、戦闘フェーズ・近接反撃を ATTRIBUTE_SPEC に委譲 |
| 1.9 | 2026-06-11 | 新規作成と編集の役割分担（§5.8）、編集時は属性・名前・レア固定・BP再算出、AI属性決定の将来方針（§5.4.2）、編集UI文言・データ保持の修正 |
| 1.7 | 2026-06-06 | デッキ戦力（属性係数合計）、上振れ勝利 EXP ボーナス、マイデッキ戦力表示を反映 |
| 1.6 | 2026-06-06 | EXP 獲得（レベル+撃破数）、BASE+L レベルアップ、プログレスバー UI を反映 |
| 1.5 | 2026-06-06 | 初回セットアップ（ユーザー名）、UserProfile 永続化、EXP 加算、ヘッダー表示を反映 |
| 1.4 | 2026-06-06 | 戦闘準備・戦闘 UI（ガイダンス帯・スロット枠・終了表示・固定フッター）、防御の自己盾付与不可・タップ操作、CPU配置アニメ、陣営別 WIN/LOSE を反映 |
| 1.3 | 2026-06-06 | マイデッキ画面（編集・並べ替え・削除）、イメージ作成/カード編集 UI（2×8 パレット・描画ツール・確認ダイアログ）、phone シェル・エディタ余白、`updateCardFromDrawing` を反映 |
| 1.2 | 2026-06-06 | 相互ダメージ・攻撃処理順・段階的演出（ダメージ/BP）・矢印端接続・カードUI統一・盾バフアイコン・補充UI文言を反映 |
| 1.1 | 2026-06-06 | 5枚全配置制、前衛/後衛、スマホ縦持ちUI、タップ操作、防御/補充/演出ルールを反映 |
| 1.0 | 2026-06-04 | 初版（対話による仕様確定を反映） |

---

*本書はプロトタイプ実装の単一の参照源（Single Source of Truth）とする。企画書と矛盾する場合、プロトタイプ範囲では本書を優先し、差分は改訂履歴に記録すること。*
